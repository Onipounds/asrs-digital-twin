# Node
node_modules/

# OPC UA X.509 certificate store — contains the bridge's PRIVATE KEY. Never commit.
pki/

# Local backup of the pre-hardening (insecure) bridge — not for the repo.
bridge.insecure.bak.js
